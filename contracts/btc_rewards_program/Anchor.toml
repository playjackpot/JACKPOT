[toolchain]
anchor_version = "0.30.1"
solana_version = "1.18.11"

[programs.localnet]
btc_rewards_program = "YourBTCProgramIDHere"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/*.ts"
